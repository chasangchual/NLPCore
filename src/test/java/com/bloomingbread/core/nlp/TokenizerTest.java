package com.bloomingbread.core.nlp;

import org.junit.Test;

public class TokenizerTest {
/*    @Test
    public void textTokenizer() {
        Tokenizer tokenizer = new Tokenizer(TestCorpusProvider.getNYTimesArticle());
    }

    @Test
    public void textTokenizerWithStopWord() {
        Tokenizer tokenizer = new Tokenizer(TestCorpusProvider.getNYTimesArticle(), true);
    }

    @Test
    public void textTokenizerTokenCount() {
        Tokenizer tokenizer = new Tokenizer(TestCorpusProvider.getNYTimesArticle());
        System.out.println(tokenizer.getTokenCount());
    }

    @Test
    public void textTokenizerTokenCountWithStopWord() {
        Tokenizer tokenizer = new Tokenizer(TestCorpusProvider.getNYTimesArticle());
        System.out.println(tokenizer.getTokenCount());
    }*/
}