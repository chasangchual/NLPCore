package com.bloomingbread.core.nlp;

import org.junit.Test;

public class ParserTest {
/*
    @Test
    public void parser() {
        TextParser textParser = new TextParser(TestCorpusProvider.getNYTimesArticle());
    }

    @Test
    public void getAvailableAnnotatorClass() {
        TextParser textParser = new TextParser(TestCorpusProvider.getNYTimesArticle());
        textParser.getAvailableAnnotatorClass().forEach(clazz -> {
            System.out.println(clazz.toString());
        });
    }

    @Test
    public void getToken() {
        TextParser textParser = new TextParser(TestCorpusProvider.getNYTimesArticle());
        TokenInfoTestHelper.showTokenInfo(textParser.getToken(0, 0));
    }
*/
}